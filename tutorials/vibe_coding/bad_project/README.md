> 这是一个**错误示范**，
>
> 1. Prompt(提示词)太过随意，没有准确描述自己的需求
> 2. 项目目录结构不符合[example_project](../../example_project)中设置的规范
> 3. 如下说明文档简陋，没有明确主要文件以及实现的功能
> 4. 没有提供较为完备的测试代码，且没有对应测试目录
> 5. 没有提供与Agent对话的`.json`文件

---

> Agent: GPT5-mini(low)
> Prompt: 写一个Java的图书管理系统。能加书、借书、还书。还要能看哪些书在。

图书管理系统（简单版）

构建与运行（在项目根目录执行）：

```bash
# 编译
javac -d out src/*.java
# 运行
java -cp out Main
```

功能：

- 添加图书（输入书名与作者）
- 借书（按ID借）
- 还书（按ID还）
- 列出所有图书
- 列出可借图书

样例：运行后输入数字选择操作。
